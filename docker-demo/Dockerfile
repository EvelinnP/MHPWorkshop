# Use the official Nginx image
FROM nginx:alpine

# Copy custom index.html to the default nginx html directory
COPY index.html /usr/share/nginx/html/index.html

# Expose port 80
EXPOSE 80

# Start Nginx (default command already in base image)
